/*

Q1. what is babel?  compiler or transpiler
It is mainly used to convert ECMAScript 2015+ code into a backwards compatible version of JavaScript in current and older browsers or environments.

Q2. return () parentehesis is mendatary used ? No, when multiple line then we use it.
Q3.when we write create.react Element, what is end up to or what behind it?  Object.





/*
/*
//App.js
    import react from 'react';
    import rectDOM,{createRoot} from "react-dom/client";
    
    const Title=()=>{
      <h1 id="title" key='h2'>
          Namste react
      </h1>
    }

    const HeaderComponents=()=>{
     return(
       <div>
            <Title/>
            <h2> Na,mste eact functional Components</h2>
            <h1> This is h2 tag</h2>
       </div>
     )
    };
    const roooReactDOM.createRoot(document.getElementById('root'));
    root.render(<HeaderComponent/>);


*/
